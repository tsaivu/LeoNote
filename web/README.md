# Frontend

Rules:

- React + Vite + TypeScript
- Flow: Page -> Component -> Hook/ViewModel -> Service -> Repository/API
- Use TanStack Query for server state
- Use React Context only for auth state
- Local dev port: `9110`
